# 数智安行｜图数据可信治理与智能流通平台讲解稿

> **使用说明**：汇报前请确保后端（port 8000）和前端（port 3000）均已启动，并以 admin 账号登录。括号内为操作提示，不需要向听众朗读。

---

## 【快速版】2 分钟讲解路线

> 适用场景：管理层、客户或评审时间有限，需要快速了解平台亮点

1. **总览驾驶舱**（15秒）：展示平台整体数据：资产数、合约数、隐私计算次数、风险事件。
2. **隐私计算 → Graph-SDP**（30秒）：选择"金融交易关系图谱"，ε=1.0，点击运行。展示三条分布曲线（真实/扰动/校正）和 L1/Hellinger/MSE 指标。
3. **VPCS 查询**（20秒）：选资产，点击"执行查询"，展示 `verify_result: true`。再点"异常校验场景"，展示 `verify_result: false`。
4. **zkGCN 推理**（20秒）：点击"执行推理"，展示 proof_hash、verify_result: true。点"异常校验场景"，`verify_result` 变为 `false`。
5. **审计链验证**（15秒）：进入审计追踪，点击"验证链完整性"，展示 `chain_valid: true`。

---

## 【完整版】5–8 分钟汇报路线

### 0. 开场（30秒）

"我们的项目叫**数智安行**，全称是'图数据可信治理与智能流通平台'。

数据要素流通面临两个核心矛盾：持有方不敢开放（隐私泄露）、使用方无法信任（结果无法验证）。我们用差分隐私、零知识证明和可验证密码学，构建了一套图数据全生命周期的可信治理平台。"

---

### 一、总览驾驶舱（45秒）

【操作：进入 Dashboard，指向统计卡片和快捷入口】

"这是平台的指挥中心。顶部显示资产数、合约数、隐私计算任务、风险事件等核心指标，实时反映平台运行状态。图表展示行业资产分布和审计趋势，帮助管理者一眼掌握全局。"

---

### 二、数据资产登记（40秒）

【操作：进入数据资产，点击某资产查看详情】

"数据流通的第一步是**确权登记**。每个图数据资产都有唯一的 asset_hash（SHA-256 内容摘要），作为权属凭证。平台自动生成图快照，记录节点数和边数，作为后续隐私计算的数据源。"

---

### 三、隐私计算实验室（120秒）

【操作：进入隐私计算实验室，依次展示 Graph-SDP 和 GS-LDP】

**Graph-SDP（度分布混洗差分隐私，60秒）**：

"这里实现了 Encode-Shuffle-Analyze 框架。用户本地用 k-RR 机制扰动自己的度数，上报后经混洗器打乱身份关联，分析端用 MLE 最大似然估计校正偏差。大家可以看到三条曲线：真实分布、扰动分布、校正后的估计分布。校正后的 L1 距离比直接扰动提升了 26% 以上。"

**GS-LDP（多统计指标本地差分隐私，60秒）**：

"GS-LDP 支持同时采集三个统计指标：度分布、三角计数和聚类系数。度分布采用对称一元编码（SUC）+ Node-LDP；三角计数用剪枝算法缓解噪声边过多的问题；聚类系数通过 Laplace 机制叠加。可以切换 Node-LDP 和 Edge-LDP 两种隐私模式。"

---

### 四、VPCS 加密路径查询（60秒）

【操作：进入 VPCS 页面，执行查询，展示验证通过；再点异常校验场景，展示验证失败】

"VPCS 实现了加密图上的可验证约束最短路径查询。图主（GO）加入虚假边混淆图结构后外包给云服务器（CS），查询用户（QU）收到结果后可用证明哈希独立验证正确性。

现在我点击**异常校验场景**，模拟云服务器返回错误结果。可以看到 verify_result 立即变为 false，说明结果完整性异常被检测到。

> **技术实现说明**：本模块当前采用哈希承诺方式完成加密图与证明链路的工程化验证，覆盖 VPCS 协议的核心交互与完整性校验；高安全等级密码原语仍需在生产环境中替换为正式密码学实现。

---

### 五、zkGCN 可验证推理（60秒）

【操作：进入 zkGCN 页面，执行推理，展示证明；再点异常校验场景】

"zkGCN 对图神经网络推理过程生成零知识证明。平台对 GCN 每一层的输出生成哈希承诺作为见证（witness），最终生成 proof_hash、pk_hash 和 vk_hash 三元组。验证者无需看到模型参数就能确认推理诚实执行。

点击**异常校验场景**，模型权重被改动后，proof_hash 与预期不符，verify_result 为 false。

> **技术实现说明**：本模块当前采用哈希承诺链完成 proof/vk/pk 结构的工程化验证，覆盖推理见证、证明摘要与完整性校验流程；正式零知识证明需在生产环境中接入真实 R1CS 与证明系统。

---

### 六、风险监控 + 审计追踪（45秒）

【操作：进入风险监控，展示风险列表；进入审计追踪，点击验证链】

"风险监控实时检测六类风险：越权访问、隐私预算超限、合约过期、验证失败等。

审计追踪实现了**哈希链式不可篡改日志**：每条日志包含 log_hash 和 prev_hash，形成链式结构。点击'验证链完整性'——系统逐条校验哈希，当前链状态 valid。"

---

### 七、行业解决方案（60秒）

【操作：进入行业解决方案，点击金融联合风控场景，启动业务流程】

"最后展示三个行业场景：金融联合风控、医疗科研共享、政务数据开放。每个场景串联完整技术流程。以金融场景为例，流程是：资产登记 → 合约授权 → GS-LDP 隐私计算 → zkGCN 可验证推理 → VPCS 加密路径查询 → 风险预警 → 审计留痕。"

---

### 结语

"数智安行将六个学术算法整合进完整的工程平台，覆盖数据资产确权、隐私计算、可验证查询和全链路审计，是一个端到端的图数据可信治理解决方案。谢谢各位老师。"

---

## 技术边界答辩 Q&A

**Q：VPCS 和 zkGCN 是否真实实现了论文中的密码学？**

A：坦诚地说，VPCS 使用哈希承诺模拟了 Threshold Paillier 加密和双线性映射证明的协议链路；zkGCN 使用哈希链模拟了 zk-SNARK/Groth16 的 proof/vk/pk 三元组结构。两者均完整展示了协议的交互逻辑、异常检测能力和角色分工，但底层密码原语仍属于当前工程版本的验证实现，不等同于生产级密码库。这是为了在当前版本中平衡可验证链路、系统复杂度和交付效率。

**Q：差分隐私算法是否严格实现了论文？**

A：Graph-SDP 实现了 ESA 框架的三个阶段（编码/混洗/校正）和 k-RR 机制，GS-LDP 实现了对称一元编码、三角计数（含剪枝）和聚类系数的 Node-LDP/Edge-LDP 双模式，GCC-SDP 和 NDKD 实现了论文核心算法流程。这些属于当前工程版本的核心实现，部分细节（如方波噪声机制、严格(ε,σ)-SDP 证明）做了工程化简化，但核心统计保障与效用评估（L1/Hellinger/MSE）完整呈现。

**Q：平台有何工程创新？**

A：将六个相互独立的算法整合进统一的 Web 平台；哈希链审计日志确保操作不可篡改；前后端分离的全栈工程实现；支持金融、医疗、政务三大行业方案的端到端业务链路。
