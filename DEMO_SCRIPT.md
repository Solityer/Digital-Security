# 比赛演示讲解词（5-8分钟）

> **使用说明**：本文档为评委演示的完整讲解稿，每段均标注建议时长。演示前请确保后端（port 8000）和前端（port 5173）均已启动，并以 admin 账号登录。括号内为操作提示，不需要向评委朗读。

---

## 开场白（约 30 秒）

尊敬的评委老师，大家好。

我们的项目叫"**数智安行**"，全称是"图数据可信治理与智能流通平台"。

当前，数据作为新型生产要素，其安全流通面临两大核心矛盾：一是数据持有方不敢"开放"——开放了就会泄露隐私；二是数据使用方无法"信任"——计算结果无从验证、操作行为无从追溯。

数智安行平台的目标，正是用差分隐私、零知识证明、可验证密码学等前沿技术，从根本上解决图结构数据流通中的隐私保护与可信计算问题，打造一个"可用、可信、可查"的数据要素流通基础设施。

下面我来带大家逐一体验平台的核心功能。

---

## 一、平台总览（约 60 秒）

【操作：点击左侧导航栏"总览"，进入 Dashboard 驾驶舱页面】

首先进入**总览驾驶舱**。

大家可以看到，页面顶部是四个核心指标卡片：**数据资产总数**、**活跃合约数**、**隐私计算任务数**，以及**待处理风险事件数**。这些数据实时反映平台的整体运行状态。

右侧是**近期风险事件趋势图**，可以直观看到过去一段时间平台检测到的异常访问、预算超支等风险分布情况。

左下区域展示了**行业分布**——当前平台管理的资产覆盖金融、医疗、政务三大行业，这也是我们后续行业场景演示的基础。

总览驾驶舱设计的核心理念是：让平台管理者在第一眼就能掌握数据资产的安全状态。它不只是一个统计仪表盘，而是整个可信治理框架的入口视角。

---

## 二、数据资产登记（约 45 秒）

【操作：点击左侧"数据资产"，进入 DataAssets 页面；点击"新建资产"按钮；填写名称"金融交易图谱"，行业选"金融"，敏感等级选"3级（高）"，添加合规标签"GDPR、个保法"；点击保存】

接下来演示**数据资产登记**。

在数据要素流通的第一步，数据持有方需要将图数据资产进行在线登记，明确资产的基本属性、行业归属和敏感等级。

平台在保存资产时，会自动计算资产内容的 **SHA256 Hash 值**，作为资产确权凭证写入记录。这相当于为数据资产颁发一张"数字身份证"——只要原始数据一字不改，Hash 永远一致；一旦数据被篡改，Hash 即失效，可供溯源举证。

【操作：点击刚创建的资产，展开"图快照"标签，可以看到图的节点和边信息】

资产创建后，平台还支持绑定**图快照**，记录图的节点列表和边列表，作为后续隐私算法调用的数据源。这一步打通了"数据登记"与"算法计算"之间的链路。

---

## 三、隐私计算实验室（约 90 秒）

【操作：点击左侧"隐私计算实验室"，进入 PrivacyLab 页面】

这是平台的**核心功能区**——隐私计算实验室，集成了四大图差分隐私算法，支持在线参数调节和实时结果可视化。

### 3.1 Graph-SDP：度分布隐私发布

【操作：确保当前在"Graph-SDP"标签；在资产选择框中选择"金融交易图谱"；将隐私预算 ε 滑块设为 1.0；点击"运行算法"按钮；等待结果加载完成】

Graph-SDP 算法来源于丁红发等人发表的研究论文，采用 **Encode-Shuffle-Analyze 框架**，将本地差分隐私与混洗机制结合，发布图数据的度分布直方图。

大家可以看到，结果区域出现了**三条分布曲线**：
- 蓝色曲线是**原始度分布**，这是真实图数据中各度值的节点比例；
- 橙色曲线是 **k-RR 局部扰动后的含噪分布**，每个节点对自身度值进行了随机响应扰动，可以看到曲线已经出现了一定噪声；
- 绿色曲线是经过**最大似然估计（MLE）校正后的最终发布分布**，通过迭代算法从含噪数据中恢复出最接近真实分布的无偏估计，精度显著优于直接发布含噪分布。

下方的效用度量指标显示 KL 散度约为 0.05，说明在 ε=1.0 的隐私保护强度下，发布的度分布与真实分布仍保持了很高的统计相似度，体现了算法"隐私保护"与"数据效用"的良好平衡。

### 3.2 NDKD：k-度匿名

【操作：点击"NDKD"标签；将 k 值设为 3；点击"运行算法"；查看输出的度序列对比和分组信息】

NDKD 算法解决了另一类隐私威胁：**度序列攻击**。即攻击者通过分析节点的度值，推断出具体个人的社交关系。

算法输出了原始度序列和匿名后的度序列，可以看到匿名图中，每一个度值至少存在 3 个（即 k=3）节点具有相同度值，满足 k-度匿名性。下方的分组信息详细展示了各个匿名组的节点分配情况。

这意味着即使攻击者掌握了"某人在图中有 5 条连接"这一辅助信息，也至少面对 3 个候选节点，无法精确定位目标个体。

---

## 四、VPCS 加密路径查询（约 90 秒）

【操作：点击左侧"加密路径查询 VPCS"，进入 VPCSQuery 页面】

接下来演示平台的第二个核心创新模块——**VPCS 可验证加密路径查询**。

【操作：在资产下拉框选择"金融交易图谱"；设置起点为 node_0，终点为 node_9；将费用阈值设为 50，时间阈值设为 30，距离约束设为 100，预算设为 200；点击"发起查询"】

在金融监管、物流路径规划等场景中，图数据持有方（银行、政府）经常需要向第三方提供路径查询服务，但又不希望暴露完整的图结构。VPCS 协议通过**四角色分布式架构**解决了这个矛盾。

【操作：结果返回后，点击"协议步骤"折叠面板，展开四角色交互流程】

大家可以看到协议的四个角色：
- **GO（图所有者）**：持有原始图，对边权重进行加密并插入哑边（Dummy Edge）混淆图结构；
- **CS（云服务器）**：存储加密图，执行路径搜索，但始终看不到真实权重值；
- **Proxy（代理）**：协助解密查询结果，并附上 HMAC 签名；
- **QU（查询用户）**：收到结果后验证 HMAC 签名，确认结果未被篡改。

页面中间显示了查询返回的最优路径、路径距离、费用和时间，以及 HMAC 证明 Hash 值，验证状态显示绿色"验证通过"。

现在来演示最关键的部分——**攻击演示**：

【操作：点击"攻击演示：篡改结果"按钮；等待结果刷新】

可以看到，当 CS 尝试篡改返回的路径权重时，由于 HMAC 校验码与篡改后的数据不匹配，验证状态立即变为**红色警告"验证失败"**，QU 得知结果不可信，可拒绝使用。

这一演示直观展示了 VPCS 协议的核心价值：**在不信任云服务器的前提下，仍然能够保证查询结果的完整性和可信性**。

---

## 五、zkGCN 可验证推理（约 60 秒）

【操作：点击左侧"可验证推理 zkGCN"，进入 ZKGCNPage 页面；选择资产；点击"运行推理并生成证明"；等待结果】

图神经网络（GNN）正在被广泛用于金融欺诈检测、医疗诊断等高风险场景。但一个核心问题是：模型的推理结果如何被第三方验证？模型参数是否被人为调整？

zkGCN 模块回答了这个问题。它基于零知识证明技术，将 GCN 的整个推理过程——从邻接矩阵的消息传递，到各层的矩阵乘法，再到最终的 Softmax 分类——全部转化为 **R1CS 约束系统**，并生成一个 **Groth16 风格的紧凑证明**。

【操作：展开"推理步骤"，可以看到各 GCN 层的输入输出维度；展开"证明摘要"，可以看到 R1CS 约束数、witness 变量数、proof hash、vk hash、pk hash 等信息】

关键点在于：任何第三方只需要持有**验证密钥（vk）** 和**公开输入的 Hash**，就可以在**不获知模型参数和输入数据**的情况下，用常数时间验证证明的有效性。这就是"零知识"的本质——证明者证明了"我知道一个满足所有约束的 witness"，却没有透露 witness 的任何内容。

【操作：点击"参数篡改演示"按钮；观察验证结果变化】

当我们模拟攻击者篡改推理结果（比如修改某节点的分类概率），可以看到证明验证立即失败，页面出现红色告警。这保证了 AI 模型推理结果的**抗篡改可信性**。

---

## 六、行业场景演示（约 60 秒）

【操作：点击左侧"行业场景演示"，进入 ScenarioDemo 页面；点击"金融联合风控"场景卡片；点击"开始演示"按钮】

平台内置了三大行业场景，每个场景对应一个完整的数据流通业务链路。

我们以**金融联合风控**场景为例。

【操作：观察场景步骤动态展开，逐步显示每个阶段的说明和执行结果】

场景模拟了以下流程：
1. 甲银行将交易图数据登记为资产，与乙银行签署数据共享合约，约定隐私预算和允许算法；
2. 双方运行 Graph-SDP，在不共享原始数据的情况下，联合发布度分布统计用于欺诈模式识别；
3. 甲银行对欺诈传导路径进行 VPCS 加密查询，验证高风险资金路径的存在性而不暴露账户图结构；
4. 全程操作自动写入哈希链审计日志，监管部门可随时抽查核验。

平台的另外两个场景——**医疗数据协作**（基因图谱差分隐私发布）和**政务图谱共享**（政务关系网络可验证查询）的演示路线与此类似，大家可以课后自行体验。

这三个场景覆盖了国内数据要素流通的主要高价值领域，也证明了数智安行平台的通用性与落地价值。

---

## 七、审计追踪（约 30 秒）

【操作：点击左侧"审计追踪"，进入 AuditTrail 页面；可以看到平台所有操作的详细日志列表】

所有操作——无论是创建资产、运行算法、发起查询，还是登录登出——都会被自动记录到**哈希链式审计日志**中。每条日志记录了前驱日志的 Hash，形成一条单向不可篡改的证据链。

【操作：点击"验证链完整性"按钮；显示"链完整性验证通过"的绿色确认】

点击"验证链完整性"，系统会从第一条日志开始，逐条重新计算 Hash 并与存储值比对，全部通过则显示链完整。

【操作：点击"篡改演示"按钮；刷新后可以看到某条日志被标记为"链断裂"红色告警】

一旦任何一条日志被修改，从被篡改节点起，所有后续日志的 Hash 验证均失败，系统立即定位到断裂位置，提供不可抵赖的篡改证据。

---

## 结语（约 30 秒）

数智安行平台集成了**六大前沿算法模型**（Graph-SDP、GCC-SDP、GS-LDP、NDKD、VPCS、zkGCN），覆盖图数据隐私发布、加密查询、可验证 AI 推理三大技术方向，配以全链路审计、RBAC/ABAC 授权、行业场景演示等工程化能力，形成了完整的"图数据可信治理"技术闭环。

本项目全部算法均有学术论文支撑，工程实现覆盖前后端全栈，具备较高的技术深度与落地价值。

感谢评委老师的聆听，我们的演示到这里结束。如有问题，欢迎进一步交流。谢谢！

---

## 附：快速问答参考

**Q1：隐私预算 ε 如何选取？**
> ε 越小隐私保护越强，但数据效用越低。实际部署中通常根据合规要求选取，如 GDPR 场景一般取 ε ≤ 1.0；效用优先场景可取 ε = 2.0。平台支持自定义调节，可在演示中实时对比不同 ε 值的效果差异。

**Q2：VPCS 和传统 TLS 加密传输有何区别？**
> TLS 保护的是传输链路，数据到达服务端后是明文的，服务端完全知晓图结构。VPCS 保护的是**数据在服务端存储和计算阶段的机密性**，即使服务端（CS）完全不可信，也无法从加密图中还原真实拓扑，且查询结果可被客户端独立验证。

**Q3：zkGCN 的证明生成效率如何？**
> 真实 Groth16 证明（如使用 snarkjs）生成时间为秒级至分钟级，取决于电路规模。本平台为演示实现，使用哈希承诺链模拟，几乎无延迟。电路规模与 GCN 层数、维度成正比，实际部署需结合 GPU 加速和约束优化。

**Q4：平台的数据是否会真正联网或上链？**
> 本演示版本完全本地运行，SQLite 数据库存储在本地，无任何网络请求到外部服务。asset_hash 和 contract_hash 字段模拟了链上存证的功能，实际生产环境可对接区块链（如 Fabric、以太坊）进行真实上链。

**Q5：平台如何防止差分隐私预算被耗尽？**
> 合约中设置了 `privacy_budget_limit`（隐私预算上限），每次执行隐私计算任务会消耗对应的 ε 量，当累计消耗超过合约限额时，系统触发 `budget_exceeded` 风险事件并阻断后续计算请求，实现隐私预算的全生命周期管控。
